'use strict';

var ctrl = require('angular').module('todo').controller;

ctrl('MainCtrl',require('./main'));
ctrl('AboutCtrl',require('./about'));
ctrl('FooterCtrl',require('./footer'));
ctrl('TodoController',require('./todo'));
